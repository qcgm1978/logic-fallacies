export default function greeter(person) {
    return "Hello, " + person;
}

let user = "Jane User";
export function greeter1(person: string) {
    return "Hello, " + person;
}
// document.body.innerHTML = greeter(user);
